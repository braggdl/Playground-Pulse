- Team Feedback Grouped by Feature and Function

- Authentication and Login UX
    + Investigate different failure messages for different types of login failures.
    + Login splash should display name, not e-mail.
    + AI: Duplicate auth toggle ID bug.

- Authorization and Security Model
    + Park Admins should not be able to create parks.
    - Password rules are enforced client side. Password rules are not enforced during password reset.
    - AI: Tighten the security model further by adding Firestore rules and custom claims for privileged roles.

- Search and Discovery
    - Search/filter implementation currently reads all parks and filters client-side. This should move to a true database-side query strategy as data grows.
    + Search currently requires login. Users should be able to search and view park info without an account; login should be required for actions.
    + Search by park location.

- Map and Park Selection Experience
    + Map view becomes a blank screen when switching between parks.
    + Default map screen should show all park pins by default.
    + Default map position should be over broad area of Livonia, Michigan
    + Parks should be selectable by clicking a map pin.
    + Add option to select or look up park location when editing a park or add park location directly with an address.
    - Investigate Leaflet vs Google Maps, including Google Maps API and billing requirements.

- Crowd and Analytics
    - 7-Day Crowd Trend date range is currently dependent on last report. Update to a rolling 7-day window with current day centered.
    - Determine how 7-Day Crowd Trend should report trend direction, not only a snapshot.
    - Report times are in UTC, not local time. Add a general conversion function for broad use.
    - Consider replacing the simple bar chart with a chart library for crowd visualization.

- Reviews and Data Aggregation
    - AI: Suggested fix is incremental update using existing reviewAggregate (count + sum) instead of full re-fetch, or use Firestore aggregation query.

- Admin UI and Invitation Lifecycle
    - Full Phase 2 Workstream 1 Admin UI implementation exists via AI. May need to coalesce with Workstream 2 into a uniform UI/page.
    - AI: Add invite lifecycle management: pending, revoked, expired, and accepted states in admin UI.

- Home Page UI and Navigation Wiring
    - Top buttons on Home page are not yet wired.
    - Need button images so they can be used as final button assets.

- Notifications and Dashboard Polish
    - Notifications button is still visible on Dashboard page.

- Deployment and Distribution
    - Host website on Firebase Hosting and adjust public directory layout via firebase.json.
    - Create QR code for website.